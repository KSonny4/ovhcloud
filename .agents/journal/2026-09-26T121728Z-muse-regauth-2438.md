# Agent journal: client-level registry auth + keeper redeploys (REGAUTH-2438)

- Started: 2026-09-26T12:01:57Z UTC (progress.log step0); finished 2026-09-26T12:17:28Z UTC (last progress.log line; lane .out `exit=0`)
- Agent/session: Meta Muse Spark (lane REGAUTH-2438)
- GitHub Issue(s): KSonny4/polymarket-wallet-finder#2438 (client-level docker auth); KSonny4/platform#16 (Slice N)
- Task: lane prompt REGAUTH-2438 — owner approval 2026-09-26 13:54 CEST ("Node-level auth (#2438)"): move registry auth to the Nomad client (docker plugin auth from Bao, never printed), reship agent config with the merged PR #23 telemetry block, restart the Nomad agent ONCE, redeploy keeper-staging then keeper without inline `auth` blocks.
- Intended outcome: node-level pulls work with the CURRENT (rotated) registry password; keeper-staging + keeper healthy; platform config PR open
- Repository: KSonny4/platform (PR #25, docs only, not merged); live node ovh-nomad-fresh (written: auth file, agent config, 1 restart, 2 redeploys)
- Starting ref: platform `origin/master` (worktree `fix/client-docker-auth-2438`)
- Branch/worktree: `fix/client-docker-auth-2438` in `~/git_projects/platform/.worktrees/regauth-2438`
- Guidance: lane prompt REGAUTH-2438; platform AGENTS.md rules 8–9; project AGENTS.md L1 informative only; Cognee recall (registry/docker auth, 15 hits)
- Status: completed (`exit=0`; nothing merged per lane rules)

Persisted by the orchestrator from the lane hand-off (the lane could not commit its own journal).

## Timeline

- 12:01:57Z — Step 0: Cognee recall (15 hits) + issues (#2438 read; #2435 rotation notes read) + committed agent config located. Incident facts: Bao `secret/projects/nomad/REGISTRY` got versions 2 and 3 at 11:43:10Z/11:43:22Z after the polymarket W3 lane leaked the old password ~11:36Z (#2435) and someone rotated it; registry alloc restarted 11:43:45Z; since then `error authenticating user "publisher"` + 401s on digest-pinned pulls. Task-level docker `auth {}` (holding the OLD value) OVERRIDES plugin-level auth, so affected jobs 401 on every fresh pull.
- 12:03:43Z — Step 1 preflight (all fail-closed checks passed): Nomad 2.0.6, 1 server alive + leader, node ready; 195 allocs saved to `allocs-before.txt`; live `/etc/nomad.d/` = nomad.hcl + gossip.hcl (encrypt only) + vault.hcl — no telemetry, no docker auth, no leave_on flags (single-server restart safe); systemd nomad User=root, KillSignal SIGTERM; `/opt/nomad/docker-auth.json` absent; 149 jobs scanned, inline-auth list saved (job/group/task names only); CURRENT Bao password verified node-side via stdin pipe only (authenticated `GET /v2/` → 200, anon → 401).
- 12:04:37Z — Step 2: `/opt/nomad/docker-auth.json` written (0600 root:root, valid JSON, host key `registry.pkubelka.cz` — the only registry host in live image refs). Two recovered faults, no secret exposure: Bao `-format=json` nests under `data.data`; `sudo install … /dev/stdin` flaked, `sudo tee` + chmod/chown worked.
- 12:05:06Z — Step 3: ONE `systemctl restart nomad` (config: docker plugin `auth { config = ... }` merged into existing block + PR #23 telemetry byte-identical; backup `nomad.hcl.bak-20260926-regauth` kept on node, never copied off; `nomad config validate` → "Configuration is valid!"). API back ~10 s (tunnel 24646 never dropped); raft leader+voter; all 23 running allocs identical (none lost/restarted); watcher REVERT lines still 8 (all pre-lane); `/v1/metrics?format=prometheus` now serves (was 415).
- 12:07:59Z — Step 4a: keeper-staging v8→v9 (eval 8597ebf8; 4 auth blocks removed in python memory; CAS first try). 12:10:57Z: v9 healthy — 4/4 tasks 60 s+, 0 restarts, Received→Setup→Started, zero 401s. Node auth proven.
- 12:12:52Z — Step 4b: keeper gate held (live was v45, not the v41 in the brief — another writer iterated v41→v45 11:48–11:59Z; redacted compare: v45 ≡ last-healthy v40 except the auth value; v41 server+zlight detour documented, v42–v44 back to server+zencli). keeper v45→v46 (eval 78cea040; 2 auth blocks removed); v46 alloc 2/2 tasks 60 s+, 0 restarts, stable; production `https://keeper.pkubelka.cz/healthz` → 200 (`/`→401 and `/health`→unauthorized are app posture, not outages).
- 12:14:17Z — Step 5: platform PR #25 open (docs only: committed config comment matches live; new `docs/09-docker-registry.md` §8 — client-level auth, no `auth` blocks in jobspecs, write procedure with path + field names only); comment on #2438 (`issuecomment-5846181704`: live changes, PR link, 20 still-inline-auth jobs, keeper-repo follow-up); short comment on platform#16 (`issuecomment-5846181819`: OLD sizes kept, wave-1 resize NOT re-applied, telemetry live). All texts token-grepped clean before posting.
- 12:17:00Z — PR #25 CI `validation` pass ×2 (~59s). 12:17:28Z — `result.md` written; lane complete.

## Decisions

- Single restart (not separate config + telemetry restarts) — approved batching; verified safe via leave_on defaults + root user + config validate.
- keeper submitted only after the v45≡v40-redacted gate passed — redeploying production on an unreviewed spec was the owner's explicit STOP condition.
- 20 jobs still carrying inline `auth` left to their owners (listed in result.md + #2438 comment); keeper-repo `-var` password follow-up flagged as a separate PR lane.

## Artefacts changed

- Live: `/opt/nomad/docker-auth.json` (new, 0600); `/etc/nomad.d/nomad.hcl` (docker auth + telemetry; backup kept); keeper-staging v9, keeper v46 (both auth-free, healthy)
- Git: platform PR #25 `fix/client-docker-auth-2438` (NOT merged) — `https://github.com/KSonny4/platform/pull/25`
- Issues: #2438 comment 5846181704; platform#16 comment 5846181819
- `$S/regauth/`: `allocs-before.txt`, `allocs-after.txt`, `inline-auth-jobs.txt`, `progress.log`, `result.md` (names/versions/IDs only, no secrets)

## Checks and evidence

- `nomad config validate /etc/nomad.d` — "Configuration is valid!" (rc 0)
- Alloc comparison 23/23 identical post-restart; raft leader+voter; metrics endpoint serves Prometheus
- keeper-staging v9 + keeper v46: 60 s+ running, 0 restarts, 0×401; keeper `/healthz` → 200
- PR checks: `nomad fmt -check`, `git diff --check`, `validate-repository.sh` green; CI `validation` pass ×2
- Token-grep (vault-token prefix, long base64/hex runs, auth-block literals, password assignments, bearer tokens) before every post/commit — clean

## Reflection and knowledge saved

- Recalled context: registry/docker-auth recall (15 hits) + #2435 rotation notes; validated live (absent auth file, 401s confirmed).
- Lesson / what worked: stdin-pipe-only password handling (`-K -`, `--password-stdin`) + in-memory spec edits keep values out of argv/logs/files; CAS with the live JobModifyIndex succeeded first try on both redeploys.
- Memory delivery: saved (lesson queued to Cognee, per lane report id `pi-1790425047-594ba88c`).

## Blockers / unresolved

- 20 jobs still carry inline `auth` (cognee, cognee-alloy, cognee-blue, cognee-ingest, cognee-snapshot, dump, ge-night-master, graph-dispatcher, graph-prep-attempt, graph-site, graph-ui, guard-heartbeat, jev-corpo-lint, keeper-probe-shell, keyround, keyround-manual, keyround-periodic, ops-run, remember-drain, traffic-count) — owner redeploys pending (handled by STALEAUTH lanes for the non-wave-1 subset).
- Keeper-repo jobspecs still render `-var` password — separate PR lane.
- Nothing merged (PR #25 open; orchestrator merges).

## Handoff

- Final status: completed
- Remaining work: none for this lane (owner follow-ups listed above belong to other lanes)
- Next safe action: STALEAUTH live-strip lanes build on the node auth proven here
- Authoritative references: `$S/regauth/result.md` + `progress.log`; PR #25; #2438 comment 5846181704; platform#16 comment 5846181819

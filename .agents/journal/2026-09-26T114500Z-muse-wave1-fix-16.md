# Agent journal: fix wave-1 watcher, finish wave 1 (WAVE1-FIX-16)

- Started: 2026-09-26 ~11:37 UTC (lane prompt mtime); last observed lane write 2026-09-26T11:45:00Z UTC (wave1 folder file mtimes); lane .out left empty (stopped; its mtime 11:38:29Z is spawn, not finish)
- Agent/session: Meta Muse Spark (lane WAVE1-FIX-16)
- GitHub Issue(s): KSonny4/platform#16 (Slice N)
- Task: lane prompt WAVE1-FIX-16 — continuation of WAVE1-16 (stopped 13:29 CEST for the watcher bug): fix `watch_wave1.py` trip rules, refresh `rollback.json` targets, prove with real unit tests, hand over to the fixed watcher (gap < 60 s), apply the remaining 9 wave-1 jobs, write `result.md` + ONE #16 table comment.
- Intended outcome: fixed watcher live; all 13 wave-1 jobs on NEW sizes; result.md + #16 table
- Repository: none written (no Git commits in this lane per hard rules); live Nomad node ovh-nomad-fresh
- Starting ref: live state at 13:31 CEST per lane prompt (NEW: ge-night-master v1, graph-site v12, flags-listener v2, cloudflared-unleash v1; false-reverted OLD: tunnel-ge-edge v5, graph-dispatcher v2, keeper-staging v5; never applied: dump, graph-ui, jev-corpo-lint, keeper-alloy, unleash, unleash-postgres; oversubscription ON; node 23459 MiB total / 16190 MiB available)
- Branch/worktree: none (live lane)
- Guidance: lane prompt WAVE1-FIX-16 (overrides WAVE1-16 where stated); project AGENTS.md L1 informative only
- Status: stopped after watcher handover (Steps 1–4 evidenced; Step 5 apply of remaining 9 jobs not completed — `apply_results.json` still holds only ge-night-master; no `result.md`, no #16 comment in hand-off sources)

Persisted by the orchestrator from the lane hand-off (the lane could not commit its own journal).

## Timeline

- ~11:37 UTC — Lane spawned (prompt + empty .out; .out never filled).
- Step 1, watcher fix — Rewrote trip rules in `$S/wave1/watch_wave1.py`: job-level trip only on real OOM ("OOM Killed"/docker oom/kernel oom_kill with alloc DesiredStatus == run, typically followed by Restarting), restart-count increase on DesiredStatus run, or failed deployment/health check; NEVER on Killing/Sent-interrupt/Terminated-Exit-0/Killed, DesiredStatus stop/evict, job-update/migrate descriptions, or JobVersion < current Version; reverts via a single worker-thread queue (event handling never blocks); node-level trips unchanged except node OOM must be a real kernel oom_kill on a non-stopping container.
- Step 2, rollback refresh — Backed up `rollback.pre-fix.json`; refreshed `rollback.json` (tunnel-ge-edge, graph-dispatcher, keeper-staging Version/JobModifyIndex set to current live values) after a live-vs-manifest check.
- Step 3, real tests — Replaced stub `test_watch.py` with a real `unittest` suite; `test_watch.out` records 27 tests, all OK: MUST-NOT-TRIP (tunnel-ge-edge 9845eb7c job-update Exit 0; keeper-probe-shell 6577f279 DesiredStatus-stop Exit-137 "OOM Killed"; Killing on old JobVersion; dispatcher/staging Sent-interrupt during replace), MUST-TRIP (synthetic OOM on running alloc + Restarting; restart-count increase on running alloc; failed deployment), node (MemAvailable < 10% trips, 69% does not), revert queue (handling never blocks on slow revert).
- Step 4, handover — Old watcher (pid 37829) SIGTERMed; fixed watcher started detached (nohup, same `watch.log`, new `watch.pid` 52585; `watch.nohup.out` 11:45Z). Watcher still alive at persist time: heartbeat 13:10:34Z, stream connected, reverted list `['graph-ui', 'keeper-staging']`.
- Old-watcher tail (from `watch.log`, before handover): graph-ui revert STARTED 11:45:46Z (deployment failed, progress deadline → target v43, no revert-result line logged); keeper-staging revert STARTED 11:48:00Z (restart-count zlight 0→1, alloc 818e93e6 → target v5, no revert-result line logged). Total: 8 REVERT lines in `watch.log`.
- Step 5 not completed — no evidence of the remaining 9 applies (order: graph-dispatcher, tunnel-ge-edge, keeper-staging, graph-ui, jev-corpo-lint, keeper-alloy, dump, unleash-postgres, unleash last); Step 6 `result.md` + #16 comment absent from hand-off sources.

## Decisions

- Revert targets for the three false-reverted jobs follow the CURRENT live versions (they run OLD sizes, so live = correct revert target) — per lane Step 2.
- `nomad job revert` never re-applied by hand in this lane; if the watcher reverted a job, no re-apply (per Step 5 rules); more than one revert would STOP the apply — apply never started.
- Tokens only via env from Bao; wave-1-only job scope; no Git commits; wave 2 excluded — per hard rules, no deviations recorded.

## Artefacts changed

- `$S/wave1/watch_wave1.py` (fixed trip rules + async revert queue), `$S/wave1/test_watch.py` + `$S/wave1/test_watch.out` (27/27 OK), `$S/wave1/rollback.json` (refreshed) + `$S/wave1/rollback.pre-fix.json` (backup), `$S/wave1/watch.pid` (52585), `$S/wave1/watch.log` (appended; 8 REVERT lines total), `$S/wave1/apply_wave1.py` (present; Step 5 use not evidenced)
- Live: fixed watcher running (pid 52585); oversubscription left ON (untoggled per prompt; node-level revert path retained). No job resizes applied in this lane.

## Checks and evidence

- `/opt/homebrew/bin/python3.14 -m unittest -v test_watch` in `$S/wave1` — 27 tests, OK (`test_watch.out`)
- Fixed-watcher startup line + heartbeats in `watch.log` (latest 13:10:34Z, stream True) — handover confirmed
- Live-vs-manifest check before rollback refresh — passed per lane flow (refresh committed)
- No secrets in lane artefacts (tokens via env only)

## Reflection and knowledge saved

- Lesson / what worked: DesiredStatus + JobVersion gating plus an async revert queue is the fix for false-trip cascades; CAS discipline (spec JobModifyIndex, never a header index) recurs as a theme in sibling lanes.
- Memory delivery: none recorded in hand-off sources.

## Blockers / unresolved

- Remaining 9 wave-1 applies + `result.md` + #16 table — not done in this lane; needs a continuation lane with the fixed watcher live.
- graph-ui (11:45:46Z) and keeper-staging (11:48:00Z) revert-starts have no revert-result lines — outcome of those two reverts is unconfirmed in the sources.
- No failing-check weakening: n/a (no repo checks in this lane).

## Handoff

- Final status: stopped (after Step 4 handover); wave-1 apply incomplete
- Remaining work: Step 5 (9 remaining jobs, one at a time, no re-apply after a revert, STOP on >1 revert) + Step 6 (result.md, #16 table comment)
- Next safe action: verify fixed-watcher quiet + current live versions, then resume Step 5 order starting at graph-dispatcher
- Authoritative references: `$S/WAVE1-FIX-16.md`; `$S/wave1/test_watch.out` (27 OK), `rollback.json` / `rollback.pre-fix.json`, `watch.pid` (52585), `watch.log` (8 REVERT lines; heartbeat 13:10:34Z)

# Agent journal: Slice N Nomad memory baseline via API (BASELINE-16)

- Started: 2026-09-26 ~10:31 UTC (per lane hand-off); finished 2026-09-26T10:57:43Z UTC (lane report mtime)
- Agent/session: Meta Muse Spark (lane BASELINE-16)
- GitHub Issue(s): KSonny4/platform#16 (Slice N: "right-size Nomad memory via oversubscription, then watch")
- Task: lane prompt BASELINE-16 — READ-ONLY baseline: nodes, running allocs + reservations, 3 usage samples ~10 min apart, proposed sizes per formula, wave assignment; write scratchpad JSON+MD; post ONE comment on #16; no Nomad writes, no repo commits, no sizes applied
- Intended outcome: a data-backed per-task sizing table and wave-1 saving projection on issue #16
- Repository: KSonny4/platform (untouched — repo `git status` unchanged: 3 pre-existing untracked entries only)
- Starting ref: not recorded in the hand-off (read-only lane; no checkout changes)
- Branch/worktree: none (helper code + raw data only under the orchestrator scratchpad `scratchpad/lanes/baseline/`)
- Guidance: repo AGENTS.md; lane access rules (scoped agent-reader token only, >= 4 s spacing, 8/16/32 s backoff on 429)
- Status: completed (baseline posted; key finding: the Nomad stats API path yields no usable data)

Persisted by the orchestrator from the lane hand-off (the lane could not commit its own journal).

## Timeline

- ~10:31–10:55 UTC — Read-only collection, all Nomad GETs spaced >= 4 s (no 429s hit): `NOMAD_ADDR` via the owner SSH tunnel (tunnel process untouched); scoped agent-reader token from Bao `secret/projects/nomad/AGENT_READ_TOKEN` only (never read `NOMAD_BOOTSTRAP` or any other token; values never printed). Endpoints: `/v1/nodes` (1 node `ovh-nomad-fresh`), `/v1/node/<id>` (memory total 23458 MiB, reserved 0), `/v1/allocations?namespace=*&resources=true` (474 total, 25 running: 24 `default` + 1 `trading`; 37 tasks), `/v1/client/allocation/<id>/stats` x3 (~10 min apart, 25 allocs each, ~100 s per sweep; samples ~10:32/10:42/10:52 UTC), `/v1/client/stats` x2 (host Used ~6 GiB).
- ~10:55 UTC — Analysis: per-alloc stats return zeros + empty `Tasks` + `Timestamp=0` for EVERY alloc on EVERY sample (known docker-stats failure mode, cf. hashicorp/nomad#26166). Formula `memory = max(64, ceil(max_obs*1.5/16)*16) MiB`, `memory_max = max(old memory, old memory_max, 2*new memory)`, skip if saving < 64, never shrink openbao, CPU unchanged — would yield 64 MiB from zero-observations, which is unsafe, so every task is SKIP and reservations stay unchanged.
- ~10:55 UTC — Posted one issue comment: https://github.com/KSonny4/platform/issues/16#issuecomment-5845657842 (node table, 37-row per-task table, wave-1 total, formula; no token/env values).

## Decisions

- All-SKIP verdict: with `max_obs = 0/unknown` for every task there is no evidence for any shrink; wave-1 projected saving = 0 MiB; registry 256 → 128 NOT supported (observed 0/unknown, no evidence it fits).
- Conflict resolved from prior context: 23 GB is the real node total (23458 MiB live); the 7.7 GB figure was free memory on 2026-09-25, not total.

## Artefacts changed

- (No repo changes.) Scratchpad outputs: `scratchpad/lanes/baseline/baseline.json` (37 rows + node totals + formula), `baseline.md` (tables), `raw/` (nodes, node detail, allocs, stats1/2/3, host-stats), helper code `collect.py` + `analyze.py` (Python 3.14 stdlib only, spacing/backoff as specified).
- Issue comment 5845657842 on KSonny4/platform#16.

## Checks and evidence

- Node free memory before: 4218 MiB (`ovh-nomad-fresh`: total 23458 − reserved 0 − running reservations 19240 MiB).
- Usage samples: all three return `RSS=0 Swap=0 Usage=0`, `Tasks={}`, `Timestamp=0` cluster-wide.
- OOM history: 0 `OOM Killed` events across all 37 tasks. Restarts: 2 tasks with Restarts=1 (`dump/app`, `control-panel/server`); rest 0. All tasks MemoryMaxMB=0 (oversubscription off, no ceiling) — nothing near max.
- Reservation vs reality: reserved 19240 MiB vs host actual ~6.0–6.2 GiB Used → free reservation ~18%.
- No polymarket `rec.shadow` alloc running. Unclassified (not in wave 1): `cloudflared-unleash`, `ge-night-master` (strict `graph-*` prefix), `keeper-alloy`, `keeper-probe-shell` (strict exact `keeper`), `tunnel-ge-edge`, `dom.ops-serve` x2.

## Reflection and knowledge saved

- Recalled context: not recorded in the hand-off.
- Lesson / what worked: an all-zeros `client/allocation/stats` sweep across every alloc and sample means the stats pipeline itself is broken (nomad#26166 docker-stats mode), not that the cluster is idle — never size from zeros; require an alternate measurement first.
- Memory delivery: not recorded in the hand-off for this lane.

## Blockers / unresolved

- Nomad per-alloc stats pipeline returns no data — the API path cannot back any resize. Follow-up needed (separate step, not this lane): fix Docker stats collection or approve an alternate per-task measurement, then recompute. (Became lane BASELINE-SSH-16.)

## Handoff

- Final status: completed (read-only; no Nomad writes, no commits)
- Remaining work: re-measure per-task usage via an alternate path before any resize; oversubscription/telemetry/size changes explicitly out of scope
- Next safe action: SSH read-only cgroup sampling lane against the same task set
- Authoritative references: issue comment 5845657842; scratchpad `lanes/baseline/{baseline.json,baseline.md,raw/,collect.py,analyze.py}`

# Agent journal: Nomad telemetry for Slice N (TELEMETRY-16)

- Started: 2026-09-26T11:08:00Z
- Agent/session: Meta Muse Spark (lane TELEMETRY-16)
- GitHub Issue(s): KSonny4/platform#16 (Slice N step 2 / step 7 telemetry follow-up)
- Task: ONE repo PR (no live change, no merge) — telemetry block in config/nomad.hcl, Alloy scrape job under jobs/, docs section in docs/03-nomad.md, this journal
- Intended outcome: `config/nomad.hcl` exposes Prometheus metrics; `jobs/nomad-metrics-alloy.nomad.hcl` ships them to Grafana Cloud; docs give apply + rollback
- Repository: KSonny4/platform
- Starting ref: b7cab78 (origin/master, PR #18 merged)
- Branch/worktree: feat/nomad-telemetry-16 in ~/git_projects/platform/.worktrees/telemetry-16
- Guidance: KSonny4/engineering-guidance@474b8c21108d06309c6dde6bd492f200779a84e4 (AGENTS.md + agent-journal.md standard + journal template; operations/secrets playbooks as triggered for Bao/Nomad handling)
- Status: completed (PR open, CI green, unmerged per lane rules)

## Timeline

- 2026-09-26T11:08Z — Cognee recall (Nomad telemetry Alloy Grafana pattern); key hit: Alloy sidecar must render to task-relative `local/config.alloy`, password via `env(...)` + task env, never baked.
- 2026-09-26T11:0xZ — Created worktree from origin/master (branch feat/nomad-telemetry-16). Confirmed starting ref b7cab78 carries config/nomad.hcl (PR #18).
- 2026-09-26T11:0xZ — Read Alloy pattern from feat/dump-grafana-alloy (commit 88da504: digest-pinned image, remote_write to prometheus-prod-55, basic_auth user 2961502, -var from Bao path, `local/config.alloy`).
- 2026-09-26T11:0xZ — Bao metadata read: secret/projects/nomad/GRAFANA_CLOUD_RW2 has single field `token` (names only, no values).
- 2026-09-26T11:0xZ — Live reads via agent-reader token, ~4s spacing: keeper-alloy + cognee-alloy specs (host network, task-relative template, same remote_write shape). Live keeper-alloy Env shows resolved literals — noted as submit-time -var resolution, NOT a pattern to copy into Git.
- 2026-09-26T11:07Z — Docs check: Nomad /v1/metrics API requires NO ACL ("ACL Required: none"). Live unauthenticated probe returned 415 "Prometheus is not enabled" (not 403) — proves no token needed AND that telemetry is currently off. Telemetry field names verified against Nomad telemetry reference; Alloy scrape args (metrics_path, params) verified against Alloy prometheus.scrape reference.
- 2026-09-26T11:08Z — Wrote: telemetry block in config/nomad.hcl (pure insertion), jobs/nomad-metrics-alloy.nomad.hcl (new), docs/03-nomad.md section 8 (new). Offline validation: `nomad fmt -check` clean, `nomad job validate` successful, `git diff --check` clean, secrets grep on diff clean (no values).

- 2026-09-26T11:09Z — Full repo check `bash scripts/validate-repository.sh` passed (iac, shellcheck, retired-plane gate, terraform validate, graft check OK). gitleaks not installed locally; CI runs it.
- 2026-09-26T11:1xZ — Committed fc423e5, pushed branch, opened PR KSonny4/platform#23 vs master (refs #16). CI `validation` passed on both triggers (58s, 1m12s). NOT merged per lane rules.

## Decisions

(No change — decisions as recorded at implementation time.)

- Scrape needs NO Nomad token — Nomad API docs ("ACL Required: none") + live 415-vs-403 behaviour. Documented in job header and docs.
- Credential mechanism: HCL2 `-var="grafana_cloud_rw2_token=..."` from Bao path secret/projects/nomad/GRAFANA_CLOUD_RW2 (field `token`), wired to task env + `sys.env()` in Alloy config — same mechanism as the dump Alloy task. No value in Git.
- Image: digest-pinned `grafana/alloy:v1.19.2` (same version as live keeper-alloy, pinned like the dump task). Resources: cpu 100, memory 128, memory_max 256 (memory_max takes effect with the Slice N oversubscription step; accepted by validate regardless).
- Scrape 30s vs collection 10s satisfies the Nomad guidance (collection <= scrape). Apply order: telemetry config + agent restart FIRST (until then the scrape 415s), then submit the Alloy job.
- Telemetry block placed between `acl` and `plugin "docker"`; everything else byte-identical (diff: +12/-0).

## Artefacts changed

- config/nomad.hcl (+12 lines: comment + telemetry block)
- jobs/nomad-metrics-alloy.nomad.hcl (new)
- docs/03-nomad.md (+39 lines: section 8)
- .agents/journal/2026-09-26T110853Z-muse-telemetry-16.md (this file)

## Checks and evidence

- `nomad fmt -check config/nomad.hcl jobs/nomad-metrics-alloy.nomad.hcl` — exit 0
- `nomad job validate jobs/nomad-metrics-alloy.nomad.hcl` — "Job validation successful" (driver config unchecked offline, expected without agent)
- `git diff --check` — clean
- Secrets grep over diff + new file — no token/password/secret values (only path/field names and the pre-existing numeric Cloud usernames)
- Live read-only probes (agent-reader token, ~4s spacing): job list, keeper-alloy + cognee-alloy specs, unauthenticated /v1/metrics (415, no 403)

## Reflection and knowledge saved

- Recalled context: Alloy sidecar gotchas (task-relative template, env-based password) — directly applied; dump-task diff confirmed the exact shape.
- Lesson: a live 415 "Prometheus is not enabled" on an unauthenticated /v1/metrics call is dual evidence — no ACL gate (else 403) plus telemetry-off baseline. Worth reusing for future endpoint gating questions.
- Memory delivery: no-new-learning (lane-local findings recorded here; no reusable cross-repo lesson beyond already-indexed Alloy wiring memory).

## Blockers / unresolved

- None. Remaining: run repo checks, commit, open PR vs master (refs #16), watch CI validation (max 15 min), no merge.

## Handoff

- Final status: completed (awaiting owner review/apply; lane forbids merging)
- Remaining work: none in this lane. Owner/operator edge applies: reship config + `systemctl restart nomad`, then `nomad job run` the Alloy job with the Bao-sourced token (exact steps in docs/03-nomad.md §8).
- Next safe action: owner reviews PR #23; follow-up Slice N lanes handle oversubscription + watch
- Authoritative references: PR https://github.com/KSonny4/platform/pull/23, head fc423e5, CI validation pass; issue KSonny4/platform#16

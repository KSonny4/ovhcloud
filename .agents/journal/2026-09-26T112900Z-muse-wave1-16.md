# Agent journal: Slice N wave-1 right-size (WAVE1-16)

- Started: 2026-09-26 ~11:14 UTC (lane prompt mtime); finished/stopped 2026-09-26T11:29:00Z UTC (stop time stated in successor prompt WAVE1-FIX-16.md; lane .out left empty)
- Agent/session: Meta Muse Spark (lane WAVE1-16)
- GitHub Issue(s): KSonny4/platform#16 (Slice N)
- Task: lane prompt WAVE1-16 — owner pre-approval 2026-09-26 12:06 CEST, re-confirmed 13:12 CEST ("Go: auto-revert"): snapshot rollback state, start an OOM safety watcher before any change, turn on Nomad memory oversubscription, apply the wave-1 shrink rows by formula, report per-job results. The documented exception to watcher-only deploys.
- Intended outcome: wave-1 jobs right-sized with auto-revert protection; per-job table on #16
- Repository: KSonny4/platform (untouched by this lane — no Git commits per lane rules); live Nomad node ovh-nomad-fresh (written via Nomad API + SSH reads)
- Starting ref: n/a (live lane; inputs from $S/baseline-ssh/baseline.json, 17 wave-1 shrink rows)
- Branch/worktree: none (live lane, no repo checkout)
- Guidance: lane prompt WAVE1-16 (strict step order, never-touch list, 2–4 s API spacing); project AGENTS.md L1 informative only
- Status: stopped mid-lane (by orchestrator at 13:29 CEST / 11:29Z) after a watcher bug caused false reverts; continued by lane WAVE1-FIX-16

Persisted by the orchestrator from the lane hand-off (the lane could not commit its own journal).

## Timeline

- ~11:14 UTC — Lane spawned (prompt + empty .out created; .out mtime 11:14:22Z is spawn, not finish — the lane demonstrably ran until 11:29Z).
- Before any change — Wrote `$S/wave1/watch_wave1.py` (stdlib + nomad/ssh/bao CLIs) and rollback snapshot `$S/wave1/rollback.json` (scheduler config incl. MemoryOversubscriptionEnabled; per wave-1 job namespace, Version, JobModifyIndex, per-task MemoryMB/MemoryMaxMB; node reserved memory BEFORE). Started the watcher detached (nohup, 24 h self-exit); `watch.log` startup line 11:20:32Z covers 13 wave-1 jobs (cloudflared-unleash, dump, flags-listener, ge-night-master, graph-dispatcher, graph-site, graph-ui, jev-corpo-lint, keeper-alloy, keeper-staging, tunnel-ge-edge, unleash, unleash-postgres), oversub snapshot false; heartbeat confirmed live before changes.
- After watcher live — Set `nomad operator scheduler set-config -memory-oversubscription=true` (submit token via env only) and verified via get-config.
- Wave-1 apply, one job at a time — `apply_wave1.py` (start from live spec, memory-only diff via `nomad job plan`, submit with `-check-index`): ge-night-master applied (recorded in `apply_results.json`: "applied, status=None stable=None allocs-running"). Other applies in flight when the watcher tripped.
- 11:28:24Z — Watcher FALSE revert: tunnel-ge-edge, alloc 9845eb7c killed with "alloc is being updated due to job update", Exit Code 0; reverted to v3 (OLD values; now v5).
- 11:29:20Z — Watcher FALSE revert: graph-dispatcher on "Killing ... Sent interrupt"; reverted to v0 (OLD values; now v2).
- 11:29:50Z — Watcher FALSE revert: keeper-staging on "Sent interrupt"; reverted to v3 (OLD values; now v5).
- 11:29Z (13:29 CEST) — Orchestrator stopped the lane because of the watcher bug. No `result.md` written; no #16 table comment posted by this lane; no Git commits (forbidden in this lane).

## Decisions

- Watcher bug causes (recorded in successor prompt WAVE1-FIX-16.md): (1) trips on any "Killing: Sent interrupt" outside a detected deploy window; (2) deploy-window detection lags ~40 s behind the version change; (3) serial event processing blocks 15–20 s on synchronous `nomad job revert`, cascading. "OOM Killed" text alone is not sufficient evidence (keeper-probe-shell alloc 6577f279, DesiredStatus stop, "alloc not needed due to job update": Killing → 5 s later Terminated Exit 137 "OOM Killed" → Killed — a forced kill during routine replace, outside wave 1).
- Live sizes at stop (per successor prompt): NEW/applied kept — ge-night-master v1, graph-site v12, flags-listener v2, cloudflared-unleash v1; OLD after false revert — tunnel-ge-edge v5, graph-dispatcher v2, keeper-staging v5; never applied — dump, graph-ui, jev-corpo-lint, keeper-alloy, unleash, unleash-postgres.

## Artefacts changed

- `$S/wave1/watch_wave1.py`, `$S/wave1/apply_wave1.py`, `$S/wave1/rollback.json` (created; live-side artefacts, not in Git)
- `$S/wave1/watch.log` (watcher JSON lines; 6 REVERT lines for the three false reverts, all rc=0), `$S/wave1/watch.pid` (old watcher pid 37829)
- `$S/wave1/apply_results.json` (one entry: ge-night-master applied)
- Live: memory oversubscription ON; 4 jobs on NEW sizes; 3 jobs false-reverted to OLD sizes. Watcher left running (pid 37829).

## Checks and evidence

- Watcher unit tests (`python3.14 -m unittest`) before start — passed per lane prompt (stub suite; replaced with real tests by WAVE1-FIX-16)
- `nomad job plan` resources-only diff gate per job — applied to ge-night-master; other jobs skipped/reported after the false reverts
- Watcher heartbeat + stream-connected INFO lines — live before Step 2
- No secrets in any lane artefact (tokens via env only, per lane rules)

## Reflection and knowledge saved

- Recalled context: lane prompt required no Cognee recall for WAVE1-16 (Step 0 is snapshot, not recall).
- Lesson / what worked: a revert-on-Killing watcher without DesiredStatus/JobVersion gating false-trips on normal redeploy shutdowns; successor lane encodes the fix (trip only on DesiredStatus run + real OOM/restart-count/deployment-failure; async revert queue).
- Memory delivery: none recorded in hand-off sources (no `cognee-guard remember` line in this lane's materials).

## Blockers / unresolved

- Watcher bug (false reverts above) — handed to WAVE1-FIX-16, not fixed in this lane.
- Per-job table + #16 comment + `result.md` — not produced; pending successor lane.
- No failing-check weakening: n/a (no repo checks in this lane).

## Handoff

- Final status: stopped (orchestrator halt at 11:29Z); continued by WAVE1-FIX-16
- Remaining work: fix watcher, refresh rollback targets, apply remaining wave-1 jobs, write result.md + #16 table
- Next safe action: WAVE1-FIX-16 Step 1 (fix trip rules) before any further apply — executed by successor lane
- Authoritative references: `$S/WAVE1-16.md`; `$S/wave1/rollback.json`, `watch.log` (startup 11:20:32Z, 3 false reverts 11:28–11:29Z), `apply_results.json`; successor prompt `$S/WAVE1-FIX-16.md` ("What went wrong")

# Environment / application secrets
.env
.env.*
!.env.example

# Private keys and certificates
*.pem
*.key
id_rsa
id_ed25519
*_rsa
*_ed25519

# Provider / tooling credentials
ovh.conf
.ovh.conf
credentials.json
*.secret
secrets.json

# Local-only state
private/
local/
backups/
*.local
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Terraform local state, provider cache and operator-only inputs
**/.terraform/
*.tfstate
*.tfstate.*
tfplan*
terraform.tfvars
backend.hcl
imports.tf
.fresh-handoff-*.json
infra/terraform-fresh/main.tf
infra/terraform-fresh/imports.tf
*.tfvars
!*.tfvars.example
!backend.hcl.example
!imports.tf.example
